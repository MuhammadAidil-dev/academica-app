import AuthenticateLayout from '../../components/layouts/authenticate/AuthenticateLayout';
import LoginSection from '../content/authenticate/LoginSection';

const LoginPage = () => {
  return (
    <AuthenticateLayout>
      <LoginSection />
    </AuthenticateLayout>
  );
};

export default LoginPage;
